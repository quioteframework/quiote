<?php
namespace Sandbox\Models;

/**
 * Lives where a model lives and resolves like one, but does not extend
 * {@see \Quiote\Model\Model} -- so the locator has something to reject.
 */
class NotAModel
{
}
